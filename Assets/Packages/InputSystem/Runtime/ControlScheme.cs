namespace Input
{
    /// <summary>
    /// An enum that represents what control scheme the player is using to play the game.
    /// </summary>
    public enum ControlScheme
    {
        /// <summary>
        /// Keyboard and mouse.
        /// </summary>
        Keyboard,
        /// <summary>
        /// Any type of controller.
        /// </summary>
        Gamepad,
    }
}
